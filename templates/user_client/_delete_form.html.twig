<form method="post" action="{{ path('user_client_delete', {'id': user_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_client.id) }}">
<button class="btn btn-danger btn-sm">Supprimer le compte</button>

</form>
